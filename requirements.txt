# Cmvng Bot — Railway deploy requirements
# These four are imported at module level: if any is missing the app
# fails to BOOT (not just a degraded feature). Keep them in your repo.
Flask>=3.0
pg8000>=1.30
requests>=2.31
beautifulsoup4>=4.12
# Imported inside the Chainlink RTDS thread only — if missing, the app
# still boots and trades; only the real-time price stream is disabled.
websocket-client>=1.7
# Cloudflare bypass for Sofascore/FBref (real Chrome TLS fingerprint).
# Graceful fallback to plain requests if missing — app still boots either way.
curl_cffi>=0.7.0
# Cloudflare JS-challenge solver (pure Python, no browser) for FBref/Sofascore.
cloudscraper>=1.2.71
# Polymarket CLOB client — the V2 crypto engine needs this to READ Polymarket
# prices and PLACE Polymarket trades. WITHOUT it the app still boots and
# Limitless still works, but the Polymarket price read returns nothing, so the
# engine skips every Polymarket setup ("no book data, skip") and never enters
# or notifies — which is exactly why only Limitless has been firing.
# Pinned to a known-good version that matches the import paths the code uses
# (py_clob_client.client.ClobClient, py_clob_client.clob_types.ApiCreds).
# NOTE: this pulls eth-account>=0.13, web3, eth-abi, eth-utils — exactly the
# packages the inlined Limitless SDK code needs at runtime. No separate
# py-limitless install required.
py-clob-client==0.34.6
# Limitless SDK is INLINED inside app.py (search for "LIMITLESS EXCHANGE SDK"
# in the file). py-limitless==0.1.1 used to be here, but its hard pin of
# eth-account==0.10.0 conflicted with py-clob-client's eth-account>=0.13.0.
# The SDK code itself works fine on the newer eth-account — only the pip
# metadata pin was wrong — so we inlined the SDK source.
